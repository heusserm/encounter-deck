<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ draws the system splash screen, so give it the app's
         colour and logo rather than the default launcher-icon treatment. -->
    <style name="Theme.EncounterDeck" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_logo</item>
    </style>
</resources>
